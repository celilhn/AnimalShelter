@model List<Domain.Models.Role>
@using Domain.Models
@using static Domain.Constants.Constants
@{
    ViewData["Title"] = "List";
    Layout = "~/Views/Shared/_PanelLayout.cshtml";
}

<div class="container-fluid">

    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">

                    <h4 class="mt-0 header-title">Rol Listesi</h4>
                    <p class="sub-title">
                    </p>

                    <table id="datatable-buttons" class="table table-striped table-bordered dt-responsive nowrap" style="border-collapse: collapse; border-spacing: 0; width: 100%;">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Kayıt Tarihi</th>
                                <th>Güncelleme Tarihi</th>
                                <th>Durumu</th>
                                <th>Rol</th>
                                <th>Güncelle</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Role role in Model)
                            {
                                <tr>
                                    <td>@role.ID</td>
                                    <td>@role.InsertionDate</td>
                                    <td>@role.UpdateDate</td>
                                    <td>
                                        @if (role.Status == (int)StatusCodes.Pasif)
                                        {
                                            <p>Pasif</p>
                                        }
                                        else
                                        {
                                            <p>Aktif</p>
                                        }
                                    </td>
                                    <td>@role.Name</td>
                                    <th><a asp-controller="Role" asp-action="Update" asp-route-Id="@role.ID"><i class="fas fa-edit"></i></a></th>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>
        </div>
        <!-- end col -->
    </div>
    <!-- end row -->

</div>
<!-- end container-fluid -->
@using Domain.Models;
@model List<Domain.Models.User>
@using static Domain.Constants.Constants;
@{
    ViewData["Title"] = "List";
    Layout = "~/Views/Shared/_PanelLayout.cshtml";
}

<div class="container-fluid">

    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">

                    <h4 class="mt-0 header-title">Kullanıcı Listesi</h4>
                    <p class="sub-title">
                    </p>

                    <table id="datatable-buttons" class="table table-striped table-bordered dt-responsive nowrap" style="border-collapse: collapse; border-spacing: 0; width: 100%;">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Kayıt Tarihi</th>
                                <th>Güncelleme Tarihi</th>
                                <th>Durumu</th>
                                <th>Adı</th>
                                <th>Soyadı</th>
                                <th>Kullanıcı Adı</th>
                                <th>E-Posta</th>
                                <th>Rol</th>
                                <th>Güncelle</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (User user in Model)
                            {
                                <tr>
                                    <td>@user.ID</td>
                                    <td>@user.InsertionDate</td>
                                    <td>@user.UpdateDate</td>
                                    <td>
                                        @if (user.Status == (int)StatusCodes.Pasif)
                                        {
                                            <p>Pasif</p>
                                        }
                                        else
                                        {
                                            <p>Aktif</p>
                                        }
                                    </td>
                                    <td>@user.Name</td>
                                    <td>@user.SurName</td>
                                    <td>@user.UserName</td>
                                    <td>@user.Email</td>
                                    <td>@user.Role.Name</td>
                                    <th><a asp-controller="User" asp-action="Update" asp-route-Id="@user.ID"><i class="fas fa-edit"></i></a></th>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>
        </div>
        <!-- end col -->
    </div>
    <!-- end row -->

</div>
<!-- end container-fluid -->

